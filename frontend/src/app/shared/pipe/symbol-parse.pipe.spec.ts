import { SymbolParsePipe } from './symbol-parse.pipe';

describe('SymbolParsePipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolParsePipe();
    expect(pipe).toBeTruthy();
  });
});
